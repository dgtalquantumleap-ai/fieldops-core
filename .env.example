# FieldOps Core Environment Variables

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fieldops_core
DB_USER=postgres
DB_PASSWORD=your_password

# Server Configuration
PORT=3000
NODE_ENV=development

# Application URL (used in emails and redirects)
# For Railway: https://your-railway-app.up.railway.app
# For Local: http://localhost:3000
APP_URL=http://localhost:3000
JWT_SECRET=your_super_secret_jwt_key_change_this_to_something_random_and_at_least_32_characters_long

# Email Configuration (Gmail Example)
# Replace with your client's actual email and app password
EMAIL_USER=your_client_email@gmail.com
EMAIL_PASS=your_gmail_app_password
ADMIN_EMAIL=your_client_email@gmail.com
ADMIN_PHONE=+1-555-0000

# Optional: Custom Domain (for production)
# BASE_URL=https://yourdomain.com

# Email Service Configuration
# For Gmail: use 'gmail'
# For Outlook: use 'outlook'
# For SendGrid: use 'sendgrid'
EMAIL_SERVICE=gmail

# Web Push Notifications (FREE - no Twilio needed)
# Generate your own keys: node -e "require('web-push').generateVAPIDKeys().then(k => console.log(k))"
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_EMAIL=mailto:your_email@gmail.com
